# secrets — never commit
backend/.env
# local Claude Code config — contains machine paths + command allowlists, not for a public repo
.claude/
backend/*.db
backend/*.db-shm
backend/*.db-wal
backend/uploads/

# local backups — real data dumps + photos, never commit (scripts are kept)
backup/db/
backup/photos-*/
supabase-dump*.sql

# node
node_modules/
backend/node_modules/

# compiled frontend (generated by npm run build — do not commit)
app/*.js

# OS
.DS_Store
Thumbs.db
MemoriesLane.zip
deployGuide.md
cloudflared.exe
# tunnel config has local paths — commit cloudflared-config.yml only after replacing placeholders
# .cloudflared/