{
  "name": "memorylane-archive-worker",
  "version": "1.0.0",
  "description": "Local worker that archives case source URLs (ArchiveBox + auto-archiver + Internet Archive) and writes results to Supabase",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "pg": "^8.11.5"
  },
  "engines": {
    "node": ">=22"
  }
}
